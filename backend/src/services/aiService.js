const aiService = {
  ask: async (message) => {
    // Placeholder
    // Replace this with OpenAI or other LLM integration
    return `You asked: "${message}". This is a placeholder AI response.`;
  }
};

module.exports = aiService;
